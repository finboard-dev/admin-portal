// Status enum for companies
export enum CompanyStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive'
}

// User roles enum
export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  MANAGER = 'manager'
}

// Navigation items enum
export enum NavigationItem {
  COMPANIES = 'companies',
  COMPANY_PROFILE = 'companyProfile',
  ORGANIZATIONS = 'organizations'
}

// Panel types for right side panel
export enum PanelType {
  COMPANIES = 'companies',
  USERS = 'users'
}